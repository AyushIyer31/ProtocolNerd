__pycache__/
*.pyc
*.pyo
venv/
nerd_engine_venv/
.env
*.egg-info/
dist/
build/
.DS_Store
storage/
logs/
*.log

# Secrets — never commit. Use variables.env.example as the template.
variables.env
protocolnerd-backend/variables.env

# Local-only artifacts
data/protocols_index.backup.json
data/*.backup.json
data/backups/

# Generated at build time (baked into deploy images)
data/protocol_index.pkl
# Dense embedding index — a build artifact like protocol_index.pkl above;
# regenerated by scripts/build_dense_index.py during the Docker build.
data/protocol_dense.npy

# LaTeX build artifacts (Documentation/paper)
*.aux
*.bbl
*.blg
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
missfont.log

# Methods-judge verdict cache: rebuildable, and keyed by judge model
protocolnerd-backend/benchmarking/citation_grounded_eval/.methods_confirm_chemistry_cache.json
